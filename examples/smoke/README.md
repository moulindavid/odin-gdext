# Smoke example

This project keeps broad runtime coverage for CI. It intentionally exercises many
bindings and ownership paths in one Godot launch.

For beginner-facing usage, start with `examples/hello` or `examples/game`.
